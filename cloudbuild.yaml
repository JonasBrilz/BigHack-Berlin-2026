options:
  logging: CLOUD_LOGGING_ONLY

steps:
  - name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: gcloud
    args:
      - run
      - deploy
      - bighack-berlin
      - --source=.
      - --region=europe-west1
      - --platform=managed
      - --allow-unauthenticated
